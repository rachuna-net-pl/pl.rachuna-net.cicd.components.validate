---
.ansible_init:
  - |
    echo ""
    echo -e "\033[1;33m===>\033[0m 🔗 Inventory init"
    git config --global url."git@gitlab.com:".insteadOf "https://gitlab.com/"
    git submodule init
    git submodule update --remote --recursive
